FROM node:22-alpine

WORKDIR /app

# Install git for any git-dependent npm packages
RUN apk add --no-cache git

# Copy everything
COPY . .

# Install contract dependencies
WORKDIR /app/contracts
RUN npm install

# Back to root
WORKDIR /app
