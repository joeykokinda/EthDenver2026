FROM node:18-alpine

WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm install

# Copy everything
COPY . .

# Compile contracts to generate ABI artifacts (needed by orchestrator)
RUN npx hardhat compile

WORKDIR /app

EXPOSE 3001

CMD ["node", "orchestrator/index.js"]
